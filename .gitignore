# SPFx / Node
node_modules/
lib/
dist/
temp/
*.log

# SPFx-generated SCSS module typings (build artifact)
*.module.scss.ts

# build output
solution/
sharepoint/solution/
*.sppkg
# NOTE: dist-standalone/ is intentionally committed on this branch so the
# prebuilt classic bundle can be downloaded/uploaded without a local build.
.heft/
bundle.log
package.log
install-node8.log

# local
.vscode/settings.json
release/
